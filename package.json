{
  "name": "picgo-plugin-alist-lingge",
  "version": "1.4.0",
  "description": "使用alist作为中转并实现对各个网盘的图床应用，原作者：jinzhi0123。凌鸽修改版，支持文件重命名插件，支持同步alist文件内容，支持同步删除alist文件信息。",
  "author": "LingGe",
  "license": "ISC",
  "homepage": "https://github.com/204065248/picgo-plugin-alist",
  "keywords": [
    "picgo",
    "picgo-gui-plugin",
    "picgo-plugin",
    "alist"
  ],
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p .",
    "dev": "tsc -w -p .",
    "patch": "npm version patch && git push origin master && git push origin --tags",
    "minor": "npm version minor && git push origin master && git push origin --tags",
    "major": "npm version major && git push origin master && git push origin --tags"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "js-base64": "^3.7.4",
    "js-sha256": "^0.9.0",
    "temp-dir": "2.0.0"
  },
  "devDependencies": {
    "@types/node": "^18.8.2",
    "picgo": "1.5.0-alpha.0",
    "typescript": "^4.8.4"
  }
}
